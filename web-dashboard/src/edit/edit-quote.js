import { successToast, errorToast } from '../toasts';
import axios, * as others from 'axios'
import { useState } from 'react';

function EditQuote({quoteBeingEdited, guildId, setQuotes, quotes, setQuoteBeingEdited}) {
    function edit() {
    }

    return (<div class='edit_quote'>
        edit quote
    </div>)
}

export default EditQuote;